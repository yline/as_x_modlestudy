进程保活
1，利用系统广播拉起
开机、亮屏、灭屏、插拔有线耳机、电量充足
PS：Android 8.0以后，大部分静态广播都被移除了。而动态广播起不到进程保活的功效

2，不同app进程，相互唤醒
1）通过广播唤醒
2）通过scheme直接唤醒【具体参照scheme】

3，前台进程
启动一个前台service，并利用系统漏洞避免其在通知栏处显示Notification。
这样既能保证进程的优先级高于普通后台进程，又将用户感知降到最低。


