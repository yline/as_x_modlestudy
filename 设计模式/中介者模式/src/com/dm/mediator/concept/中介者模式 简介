定义:
1,中介者模式包装了一系列对象相互作用的方式,使得这些对象不必相互明显作用,从而使它们可以松散耦合.
2,中介者模式将对象的行为和协作抽象化,把对象在小尺度的行为上与其他对象的相互作用分开处理.

使用场景:
1,当对象之间的交互操作较多,且每个对象的行为操作都依赖彼此.
2,系统呈网状结构,可使用中介者模式,将其变成以中介者为中心的星形结构

优点:
1,解耦了Activity在Colleague之间调用关系
缺点:
1,增加了中介人和Colleague之间的耦合

Android源码:
1,Keyguard锁屏功能实现<Mediator为结尾的命名方式>

